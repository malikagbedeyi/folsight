import React from 'react'

const Requirement = ({ page, handlePage }) => {
  return (
    <div className={page === 7 ? "" : "Requirement"}>
      requirement Page
    </div>
  )
}

export default Requirement
